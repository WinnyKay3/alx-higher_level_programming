// Use jquery to add a click event handler to the dive with id red_header.
$('#red_header').click(function() {
  // Update the text color of the header to red when te div is clicked
  $('header').css('color', '#FF0000');
});
